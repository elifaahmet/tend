/** @format */

// TODO: Need to configure jest presets with @babel/preset-typescript before any of this will work

// import { themeStylesToCssString, getInputWidth, getBorderColor, getCursorColor } from './helpers';
// import defaultExtendedTheme from '../../../theme/extended-theme';
// import { calculateWithUnit, appendValuesWithUnits, interpolateStyles } from '../../../utils/transformations';
//
// jest.mock('../../../utils/transformations', () => ({
//   __esModule: true,
//   calculateWithUnit: jest.fn(() => '1px'),
//   appendValuesWithUnits: jest.fn(() => {}),
//   interpolateStyles: jest.fn(() => ''),
// }))
//
// describe('ui-kit - lib > input > helpers', () => {
//   describe('themeStylesToCssString()', () => {
//     it('should call', () => {
//       themeStylesToCssString(defaultExtendedTheme, 'default');
//       expect(calculateWithUnit).toBeCalled();
//       expect(appendValuesWithUnits).toBeCalled();
//       expect(interpolateStyles).toBeCalled();
//     });
//   });
// });

describe('temp', () => {
  it('should be uncommented once configs are updated to support mocking', () => {
    expect(true).toBeTruthy();
  });
});
